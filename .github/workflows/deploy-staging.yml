name: Deploy to Staging

on:
  push:
    branches: [develop]

jobs:
  deploy-staging:
    name: Deploy to AWS Staging
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: me-south-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/movemate-app:$IMAGE_TAG .
          docker push $ECR_REGISTRY/movemate-app:$IMAGE_TAG

      - name: Deploy to ECS Fargate Staging
        run: |
          aws ecs update-service \
            --cluster movemate-staging \
            --service movemate-app-staging \
            --force-new-deployment \
            --region me-south-1

      - name: Wait for deployment to stabilise
        run: |
          aws ecs wait services-stable \
            --cluster movemate-staging \
            --services movemate-app-staging \
            --region me-south-1
